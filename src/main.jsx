import React from "react";
import ReactDOM from "react-dom/client";
// import App from './slot/App.jsx'
import App from "./provider/App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
